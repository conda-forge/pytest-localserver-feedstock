{% set name = "pytest-localserver" %}
{% set version = "0.3.7" %}
{% set hash_type = "sha256" %}
{% set hash = "d828d79232456d0b4eb863e9de2c85699259f436a3185e39d0d5001b8c8521b0" %}
{% set ext = "tar.gz" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ ext }}
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ ext }}
  {{ hash_type }}: {{ hash }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.10
  run:
    - python
    - pytest
    - werkzeug >=0.10

test:
  requires:
    - pytest
  imports:
    - pytest_localserver
  commands:
    - py.test --help

about:
  home: https://bitbucket.org/pytest-dev/pytest-localserver
  license: MIT
  summary: pytest-localserver is a plugin for the pytest testing framework which enables you to test server connections locally.

extra:
  recipe-maintainers:
    - nicoddemus
